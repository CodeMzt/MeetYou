# Docker-local overrides for deploy/docker/compose.core-postgres.yml.
# Copy to deploy/docker/compose.env and replace every placeholder before production use.

TZ=Asia/Shanghai

POSTGRES_DB=meetyou
POSTGRES_USER=meetyou
POSTGRES_PASSWORD=replace_with_a_long_random_password
POSTGRES_HOST_PORT=55432
MEETYOU_DATABASE_URL=postgresql+psycopg://meetyou:replace_with_a_long_random_password@postgres:5432/meetyou

MEETYOU_GATEWAY_ACCESS_TOKEN=replace_with_a_long_random_token
MEETYOU_GATEWAY_HOST_PORT=8000

# Optional WeChat iLink Bot runtime overrides. Keep disabled until QR login
# and text-loop acceptance are performed for this deployment.
MEETYOU_WECHAT_ENABLE=false
MEETYOU_WECHAT_ILINK_BASE_URL=https://ilinkai.weixin.qq.com
MEETYOU_WECHAT_CHANNEL_VERSION=2.1.7
MEETYOU_WECHAT_LOGIN_QR_PATH=user/wechat-ilink-login-qr.png
MEETYOU_WECHAT_TOKEN_FILE=user/wechat_ilink_state.json
MEETYOU_WECHAT_POLL_TIMEOUT_MS=35000
