- [x] DeepSeek / OpenAI 兼容对话请求的失败已可被复现、分类并定位到具体原因
- [x] provider 400 错误已具备结构化失败分类与调试字段，而不是仅剩通用 Bad Request
- [x] 上下文预算估算已计入 provider_items、tool_calls、tool 输出与压缩摘要
- [x] Context Planner、Length Policy 与真实请求体的预算口径一致
- [x] 长上下文与多轮工具调用场景下不会再因预算低估导致请求异常
- [x] 自动压缩已保留 tool_calls、tool 输出、provider_items 与必要元数据
- [x] 压缩状态、压缩前后预算与压缩层级可通过调试接口或前端状态查看
- [x] 压缩后仍能保持多轮工具调用与后续对话的语义连续性
- [x] reasoning、answer、tool phase 与单轮模型执行边界一致
- [x] 前端不会再出现“正文已输出但 reasoning 仍未结束”的状态错乱
- [x] DeepSeek 兼容路径的 provider limit、预算保留策略与实际模型配置一致
- [x] 运行时调试接口与前端状态已暴露 provider、context limit、预算使用、压缩状态与失败分类
- [x] 调试输出已完成脱敏，不泄露完整请求内容、密钥或高敏路径
- [x] 单元测试、集成测试与前端状态验证覆盖长上下文、工具调用、压缩与流式顺序
