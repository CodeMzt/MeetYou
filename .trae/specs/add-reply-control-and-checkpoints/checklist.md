- [x] 后端已支持对进行中的回复发起稳定中断，且中断后不会继续输出新的正文或推理增量
- [x] 中断后的 stream 结束语义可区分自然完成、用户停止、控制重放与异常失败
- [x] 会话状态已显式维护 active turn 控制态、最近可重放输入与检查点元数据
- [x] 新回复开始前会创建可恢复检查点，且检查点包含恢复所需的最小状态集合
- [x] 回复时追加引导会先中断当前回复，再基于稳定检查点与合并后的意图重新生成
- [x] 追加引导不会把控制指令永久写成污染主历史的普通独立用户轮次
- [x] 重新回复可以恢复上一轮起始检查点并重新生成最近一次回复
- [x] 重新回复会清理被替换回复残留的 assistant/tool 产物，不会出现重复历史
- [x] 后端已提供查询可用检查点与回退到指定检查点的能力
- [x] 回退后会裁剪检查点之后的失效历史，并清理悬挂控制态与运行态
- [x] 网关协议已支持 stop、append guidance、regenerate、rollback 控制命令
- [x] 控制命令具备会话归属校验、幂等或重复请求处理，以及明确的 accepted / rejected / completed 语义
- [x] 调试或状态接口可查看当前 active turn、最近控制动作、stream finish reason 与检查点摘要
- [x] 调试与协议输出已完成脱敏，不泄露完整历史内容、密钥或高敏路径
- [x] 单元测试与集成测试覆盖中断、追加引导、重新回复、检查点恢复、异常回收与幂等行为
